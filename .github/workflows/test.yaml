name: Runner Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted, local]

    steps:
      - name: Test runner
        run: |
          echo "Runner picked me up!"
          hostname
          uname -a